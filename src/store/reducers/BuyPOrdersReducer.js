const initialState = {
  PendingOrders: [],
};

const BuyPOrdersReducer = (state = initialState, actions) => {
  switch (actions.type) {
    default:
      return state;
  }
};

export default BuyPOrdersReducer;
